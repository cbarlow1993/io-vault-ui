/**
 * Stub middleware for extracting authentication credentials and entitlements.
 * This is a placeholder for the actual implementation.
 */
export const extractCredentialsAndEntitlement = () => ({
  before: () => Promise.resolve(),
});
